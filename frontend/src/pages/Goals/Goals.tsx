import { FC } from 'react';
import DefaultLayout from '../../components/common/DefaultLayout';

import './Goals.scss';

const Goals: FC = () => {
  return (
    <div>
      <DefaultLayout title="Goals and budget">Some goals</DefaultLayout>
    </div>
  );
};

export default Goals;
