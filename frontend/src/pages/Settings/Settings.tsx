import { FC } from 'react';
import DefaultLayout from '../../components/common/DefaultLayout';

const Settings: FC = () => {
  return (
    <DefaultLayout title="Settings and profile">
      Here you can make settigs your ptofile
    </DefaultLayout>
  );
};

export default Settings;
