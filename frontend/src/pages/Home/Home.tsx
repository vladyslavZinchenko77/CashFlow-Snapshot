import { FC } from 'react';
import DefaultLayout from '../../components/common/DefaultLayout';

const Home: FC = () => {
  return <DefaultLayout title="home page">Home</DefaultLayout>;
};

export default Home;
