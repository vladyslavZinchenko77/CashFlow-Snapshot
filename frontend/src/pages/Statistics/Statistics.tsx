import { FC } from 'react';
import DefaultLayout from '../../components/common/DefaultLayout';

import './Statistics.scss';

const Statistics: FC = () => {
  return <DefaultLayout title="Statistics"> Some charts</DefaultLayout>;
};

export default Statistics;
